# Инструкция по публикации сайта

## 📋 Содержание
1. Подготовка к публикации
2. Изменение контактной информации
3. Обновление URL сайта
4. Сборка проекта
5. Варианты хостинга
6. Настройка SEO после публикации

---

## 1. Подготовка к публикации

### 1.1 Установка зависимостей
```bash
npm install
```

### 1.2 Проверка работы в режиме разработки
```bash
npm run dev
```
Откройте http://localhost:3000 для проверки работы сайта.

---

## 2. Изменение контактной информации

### 2.1 Редактирование переводов
Откройте файл `src/i18n/locales/ru.json` и измените:

- **Название компании**: поле `contacts.companyPlaceholder`
- **Телефон**: поле `contacts.phonePlaceholder`
- **Email**: поле `contacts.emailPlaceholder`
- **Адрес**: поле `contacts.addressPlaceholder`

### 2.2 Обновление Schema.org разметки
Откройте файл `src/pages/HomePage.tsx` и измените данные в компоненте `<SchemaOrg>`:
- `name` - название компании
- `telephone` - телефон
- `email` - электронная почта
- `addressLocality` - город
- `addressRegion` - регион

---

## 3. Обновление URL сайта

### 3.1 Sitemap
Откройте `public/sitemap.xml` и замените `https://yoursite.com/` на URL вашего домена во всех записях.

### 3.2 Robots.txt
Откройте `public/robots.txt` и обновите ссылку на sitemap.

### 3.3 SEO настройки
В файле `src/components/SEO.tsx` - URL формируется автоматически, изменений не требуется.

---

## 4. Сборка проекта

### 4.1 Production сборка
```bash
npm run build
```

Эта команда создаст папку `dist` со всеми оптимизированными файлами.

### 4.2 Предварительный просмотр сборки
```bash
npm run preview
```

---

## 5. Варианты хостинга

### 5.1 Vercel (Рекомендуется - бесплатный)

1. Создайте аккаунт на https://vercel.com
2. Подключите GitHub репозиторий
3. Vercel автоматически определит Vite проект
4. Нажмите Deploy

**Преимущества**: Автоматический SSL, CDN, бесплатный домен .vercel.app

### 5.2 Netlify (Бесплатный)

1. Создайте аккаунт на https://netlify.com
2. Перетащите папку `dist` в интерфейс Netlify (Drag & Drop)
3. Или подключите Git репозиторий

**Build settings**:
- Build command: `npm run build`
- Publish directory: `dist`

### 5.3 GitHub Pages

1. Установите gh-pages:
```bash
npm install --save-dev gh-pages
```

2. Добавьте в `package.json`:
```json
"scripts": {
  "deploy": "npm run build && gh-pages -d dist"
}
```

3. Обновите `vite.config.ts`:
```typescript
export default defineConfig({
  base: '/repo-name/', // название вашего репозитория
  // ... остальная конфигурация
});
```

4. Запустите:
```bash
npm run deploy
```

### 5.4 Обычный хостинг (Shared hosting)

1. Соберите проект: `npm run build`
2. Загрузите содержимое папки `dist` на сервер через FTP
3. Настройте .htaccess для корректной работы роутинга:

```apache
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /
  RewriteRule ^index\.html$ - [L]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /index.html [L]
</IfModule>
```

### 5.5 Nginx

Конфигурация для Nginx (`/etc/nginx/sites-available/yoursite`):

```nginx
server {
    listen 80;
    server_name yoursite.com www.yoursite.com;
    root /var/www/yoursite/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # Кэширование статических файлов
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
```

---

## 6. Настройка SEO после публикации

### 6.1 Яндекс.Вебмастер

1. Зарегистрируйтесь на https://webmaster.yandex.ru
2. Добавьте ваш сайт
3. Подтвердите права на сайт (через HTML файл или meta-тег)
4. Отправьте sitemap: `https://yoursite.com/sitemap.xml`
5. Дождитесь индексации (обычно 1-2 недели)

### 6.2 Google Search Console

1. Зарегистрируйтесь на https://search.google.com/search-console
2. Добавьте и подтвердите сайт
3. Отправьте sitemap
4. Проверьте индексацию страниц

### 6.3 Проверка SEO

Используйте инструменты:
- https://pagespeed.web.dev/ - проверка производительности
- https://validator.w3.org/ - валидация HTML
- https://search.google.com/test/mobile-friendly - мобильная версия

---

## 7. Обновление контента

### 7.1 Изменение данных о продуктах

1. Обновите файл `public/Comet_Forward_Robots_Catalog.xlsx`
2. Запустите парсинг:
```bash
npm run parse-catalog
```
3. Пересоберите проект:
```bash
npm run build
```
4. Загрузите новую версию на хостинг

### 7.2 Добавление новых изображений

Поместите изображения в папку `public/` с правильными названиями:
- `model-name.png` - основное изображение
- `model-name pd.png` - детальное изображение

---

## 8. Поддержка и обновления

### Обновление зависимостей
```bash
npm update
```

### Проверка устаревших пакетов
```bash
npm outdated
```

---

## 🚀 Быстрый старт для публикации

```bash
# 1. Изменить контактную информацию в src/i18n/locales/ru.json
# 2. Обновить URL в public/sitemap.xml и public/robots.txt
# 3. Собрать проект
npm run build

# 4. Загрузить папку dist на хостинг
# или использовать Vercel/Netlify для автоматической публикации
```

---

## 📞 Технические детали

- **Framework**: React 19 + Vite
- **UI Library**: Ant Design 5.0
- **Routing**: React Router v6
- **Локализация**: i18next
- **SEO**: React Helmet + Schema.org
- **Стили**: CSS + Ant Design Theme

---

## ⚠️ Важные замечания

1. **Перед публикацией** обязательно измените все placeholder'ы контактной информации
2. **После публикации** обновите sitemap.xml с актуальным URL
3. **Для лучшей индексации** добавьте сайт в Яндекс.Вебмастер и Google Search Console
4. **Резервное копирование**: регулярно делайте backup файла с продуктами и изображений

---

Готово! Ваш сайт готов к публикации. 🎉

